Детальний опис фіч, що були додані до програми:

    Клас Cat (клас представлення кота):
-Додано новий конструктор Cat(String name), який приймає ім'я кота та ініціалізує поле name.
-Додано поле stamina, що представляє витривалість кота, і початкове значення стамина становить 75.
-Реалізовано метод getStamina(), що повертає значення стамина кота.
-Реалізовано метод reduceStamina(int amount), який зменшує стаміну кота на вказану кількість (amount * 2), використовуючи amount як аргумент.
-Реалізовано метод canContinue(), який перевіряє, чи може кіт продовжувати змагання, тобто, чи його стамина більше нуля.
-Реалізовано метод getName(), який повертає ім'я кота.

    Клас Human (клас представлення людини):
-Додано новий конструктор Human(String name), який приймає ім'я людини та ініціалізує поле name.
-Додано поле stamina, що представляє витривалість людини, і початкове значення стамина становить 100.
-Реалізовано метод getStamina(), що повертає значення стамина людини.
-Реалізовано метод reduceStamina(int amount), який зменшує стаміну людини на вказану кількість (amount * 2), використовуючи amount як аргумент.
-Реалізовано метод canContinue(), який перевіряє, чи може людина продовжувати змагання, тобто, чи її стамина більше нуля.
-Реалізовано метод getName(), який повертає ім'я людини.

    Клас Robot (клас представлення робота):
-Додано новий конструктор Robot(String name), який приймає ім'я робота та ініціалізує поле name.
-Додано поле stamina, що представляє витривалість робота, і початкове значення стамина становить 120.
-Реалізовано метод getStamina(), що повертає значення стамина робота.
-Реалізовано метод reduceStamina(int amount), який зменшує стаміну робота на вказану кількість (amount * 4), використовуючи amount як аргумент.
-Реалізовано метод canContinue(), який перевіряє, чи може робот продовжувати змагання, тобто, чи його стамина більше нуля.
-Реалізовано метод getName(), який повертає ім'я робота.

    Клас Main (основний клас програми):
-У методі main змінено створення об'єктів Human, Cat та Robot, додаючи їхні імена в якості аргументів конструкторів.
-Додано виведення імені учасника та його початкової витривалості перед кожною змаганням.
-Для кожної перешкоди, перевірено, чи учасник може продовжити змагання, і відповідно зменшено його стаміну.

    Інтерфейс Participant:
-Додано метод int getStamina(), який повертає значення витривалості учасника.
-Додано метод void reduceStamina(int amount), який зменшує витривалість учасника на вказану кількість.
-Додано метод boolean canContinue(), який перевіряє, чи може учасник продовжувати змагання, тобто, чи його витривалість більше нуля.
-Додано метод String getName(), який повертає ім'я учасника.

    Клас Racetrack (клас представлення бігової доріжки):
-Додано метод int getLength(), який повертає довжину бігової доріжки.
-В методі overcome(Participant participant) додано зменшення витривалості учасника на відстань бігової доріжки.

    Клас Wall (клас представлення стіни):
-Додано метод int getHeight(), який повертає висоту стіни.
-В методі overcome(Participant participant) додано зменшення витривалості учасника під час перестрибування стіни.

Ці зміни дозволяють кожному учаснику мати свою власну витривалість, що розглядається при подоланні перешкод.
Кожен учасник має ім'я, що використовується під час виведення повідомлень про його дії.
Витривалість зменшується відповідно до вимог перешкоди, що додає реалістичності до гонки.
Учасник, який не може продовжувати, виходить з гонки.
Після завершення гонки виводиться повідомлення про результати для кожного учасника.